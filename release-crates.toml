[release]
version = "0.2.0"

[crates.mynd-core]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No published code, dependency, or package metadata changed."

[crates.mynd]
previous_version = "0.1.0"
version = "0.2.0"
change = "metadata"
publish = true
reason = "Publish the reconciled scope, claims, and governance documentation."
