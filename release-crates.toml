[release]
version = "0.4.0"

[crates.mynd-math]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No published code, dependency, or package metadata changed."

[crates.mynd-core]
previous_version = "0.1.0"
version = "0.2.0"
change = "code"
publish = true
reason = "Add validated dimensions, rectangles, plane layouts, output lengths, tests, and Kani proofs."

[crates.mynd]
previous_version = "0.3.0"
version = "0.4.0"
change = "code"
publish = true
reason = "Re-export the validated mynd-core v0.2.0 geometry API through the facade."
