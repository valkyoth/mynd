[release]
version = "0.5.0"

[crates.mynd-math]
previous_version = "0.1.0"
version = "0.1.0"
change = "unchanged"
publish = false
reason = "No published code, dependency, or package metadata changed."

[crates.mynd-core]
previous_version = "0.2.0"
version = "0.3.0"
change = "code"
publish = true
reason = "Add explicit sample storage, pixel layouts, chroma and alpha domains, plane relationships, tests, and Kani proofs."

[crates.mynd]
previous_version = "0.4.0"
version = "0.5.0"
change = "code"
publish = true
reason = "Re-export the validated mynd-core v0.3.0 pixel-storage API through the facade."
